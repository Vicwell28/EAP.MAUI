<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="EAP.MAUI.Views.ShellPages.HomeStorePageShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:EAP.MAUI.Views.StorePage"
    Title="Tienda en Línea">

    <Tab Title="Productos" Icon="documento.png">
        <ShellContent
            Title="Nuestros Productos"
            ContentTemplate="{DataTemplate views:StoreProductPage}"
            Icon="documento.png" />
    </Tab>

    <Tab Title="Buscar" Icon="documento.png">
        <ShellContent
            Title="Buscar Productos"
            ContentTemplate="{DataTemplate views:StoreSearchPage}"
            Icon="documento.png" />
    </Tab>

    <Tab Title="Compras" Icon="documento.png">
        <ShellContent
            Title="Mis Compras"
            ContentTemplate="{DataTemplate views:StorePurchasesPage}"
            Icon="documento.png" />
    </Tab>

    <Tab Title="Perfil" Icon="documento.png">
        <ShellContent
            Title="Mi Perfil"
            ContentTemplate="{DataTemplate views:StoreProfilePage}"
            Icon="documento.png" />
    </Tab>

</Shell>
